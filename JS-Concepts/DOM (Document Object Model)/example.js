// Browser DOM

// Represents every single html elements
// This model allows the JS to interact with the DOM elements
// or manipulate the DOM elements

// Each node has it's own methods and properties
 
// using vanilla JS

// Browser gives us window object -> in window we have document object