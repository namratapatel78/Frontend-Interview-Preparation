// Operator Precedence - which function operator to be call first
// Associativity - If two operators have same precedence then need to execute from (l to r) or (r to l)

// JS operator precedence - https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Operators/Operator_Precedence


var a = 2, b = 3, c = 4;

a = b = c

// b = c -> returns 4 -> check in console

console.log(a);
console.log(b);
console.log(c);