// Array is an ordered list

// Insertion - It depends were are we inserting
// At end => O(1)
// At beginning => O(n) => re-indexing every element of an array


// Removal - It depends
// At end => O(1)
// At beginning => O(n) => re-indexing every element of an array

// Search - O(n)
// Access - O(1)


// push - O(1)
// pop - O(1)
// shift - O(n)
// unshift - O(n)
// concat - O(n)
// slice - O(n)
// splice - O(n)
// sort - O(n * logn)
// forEach/map/filter/reduce - O(n)
